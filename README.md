# real-time-user-intent-prediction
“Real-Time User Intent Prediction System analyzes real user clickstream and browsing duration to classify intent as High, Medium, or Low. Sessions are detected automatically, and predictions are generated using trained LightGBM and CatBoost models.”
